namespace ErrorMessage {
	export const NO_USER = 'User does not exists';
	export const WRONG_EMAIL_PASSWORD_COMBINATION =
		'Wrong Password/Email combination';
	export const USER_FORBIDDEN = 'Current user role can not access this route';
}

export default ErrorMessage;
